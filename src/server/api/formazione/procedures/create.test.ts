/**
 * Tests per la logica transazionale di scrittura formazione (delete + insert formazione + insert voti)
 * sono stati spostati in `src/server/services/scriviFormazione.test.ts`,
 * che verrà creato da dick come parte del deepening del modulo formazione.
 *
 * Questo file era la suite caratterizzante pre-refactor (FASE 3).
 * La logica inline testata qui ora vive dentro `scriviFormazione`.
 */

